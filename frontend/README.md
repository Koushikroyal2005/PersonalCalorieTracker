# NutriTrack frontend

React and TypeScript client for the Personal Calorie Tracker. See the repository-level `README.md` for complete setup, architecture, environment variables, and verification commands.

```powershell
npm install
Copy-Item .env.example .env.local
npm.cmd run dev
```

Available checks:

```powershell
npm.cmd run lint
npm.cmd run build
```
